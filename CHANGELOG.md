# Changelog

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)。
格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

## [2.0.1] - 2026-09-26

### 变更

- **展示名与文案统一。** 对外（应用中心 `display_name`、桌面图标、应用列表、启动器）显示
  「风扇控制」；进入应用后的主界面显示 `NiuFan` —— 前端 `<title>`、顶栏标题、打赏弹窗
  文案，以及启动日志与 `--help` 里的名字都统一成 `NiuFan`。README 顶部标题为
  `NiuFan · 飞牛 NAS 风扇控制`。

- **打赏弹窗新增 BUG 反馈入口。** 原来那句「打赏完全自愿，不影响任何功能，也不会改变
  软件的任何行为。」一字未改，后面另起一行给出
  「如果遇到BUG，欢迎前往 Github 反馈或者添加 QQ群：818299505」：`Github` 指向
  <https://github.com/LiuFudi/NiuFan>，与 `QQ群：818299505` 一起用项目的 `--err` 红标出。
  应用介绍（`manifest` 的 `desc`，HTML）与 README 的「打赏支持」一节同步补上这一行。

- **标识一个都没动。** `appname`、桌面入口键名 `niufan.Application`、网关路径
  `/app/niufan`、配置目录 `@appconf/niufan`、`APP_NAME`、API 路径与配置 key 全部保持原样，
  所以升级后曲线、标定结果、温度源选择照旧读得到 —— 这也是本次只抬 PATCH 号的原因：
  fnOS 不允许同名同版本覆盖安装，改了内容就必须换版本号。

## [2.0.0] - 2026-09-22

### 变更

- **项目改名为 `niufan`，中文名仍为「风扇控制」。**
  包标识 `appname` 由 `fn-fancontrol` 改为 `niufan`，仓库地址变为
  <https://github.com/LiuFudi/niufan>，桌面入口、网关路径（`/app/niufan`）
  以及全部运行时路径随之变化。`display_name` 保持「风扇控制」不变。

  **这是主版本号提升的原因**：`appname` 变了，平台会把它当成**另一个应用** ——
  不能覆盖升级，配置目录也从 `@appconf/fn-fancontrol` 变成 `@appconf/niufan`。

- **首次启动会自动迁移旧配置。** 如果新配置不存在、而旧的
  `@appconf/fn-fancontrol/config.json` 还在，会把它读过来并在日志里记一行，
  所以曲线、标定结果、温度源选择都不会丢。迁移只做一次，之后以新配置为准。

## [1.10.6] - 2026-09-22

### 变更

- **README 里打赏那一节删掉了两句说明文字**，只留下「不联网、不上报、不统计点击」与
  「打赏完全自愿，不影响任何功能」。同时删掉了 `donate.json` 注释里同样性质的一句。

  **入口的行为没有任何变化** —— 依然常驻、依然红色、依然不联网。删掉的只是文字：
  原来那两句会让人生出「被索取」的感觉 —— 一句把入口不可关闭这件事摆到了台面上，
  另一句把「不会自动弹出、不会打断」这种本分当成了卖点。讲清自愿就够了。

  规则沉淀在 `打赏功能规范.md`（与 `开发规范.md` 一起放在交付存档目录）。

## [1.10.5] - 2026-09-22

### 变更

- **硬件变化检测收窄到两项**：只比较 **CPU** 与 **风扇通道集合**。标定描述的是机器上的
  风扇接口，能真正让它失效的也就是这两样 —— CPU（接口可能通过 PECI 绑在它身上）和接口
  本身。硬盘与显卡不在其中：它们来去自由（插一下午的 USB 硬盘盒也算），为此重跑一次
  标定是噪音而不是安全。

- **风扇卡片里的辅助温度源也折叠**。每张风扇卡片原本把**所有**温度源平铺成一行勾选框，
  现在 CPU / 显卡 / 硬盘直接列出，主板 / 内存 / ACPI 热区收进「其它温度源（N 路）」
  折叠组。**已经勾选了辅助源的那张卡片默认展开**，标题也会写明已选几路 ——
  折叠组里藏着一个正在生效的来源是会出事的。

## [1.10.4] - 2026-09-22

### 修复

- **标定结果重启或升级之后就看不见了**。服务端其实已经在启动时把配置里的标定读回
  `probe_results`，但**从来没有把它合并进 `/api/hardware`** —— 而检测表格读的正是这个
  接口。于是页面一刷新、服务一重启，最低/最高转速两列就变回 `–`，判定退回「未标定」，
  看起来像标定白做了。

  现在 `/api/hardware` 会把每路已保存的标定合并回通道并标记 `stored`，
  表格直接显示上次的结果与标定时间。

- **只有被勾选管理的通道才存得下标定**。探测过的通道如果没勾上，结果出了这个进程就
  没了。现在整批结果都写进配置的 `probe` 段，与哪些通道被管理无关。

### 新增

- **硬件变化会要求重新标定**。配置里记录一份硬件指纹（CPU 型号、显卡、硬盘、风扇
  通道），每次启动比对：对不上就把 `setup_complete` 置回 False，向导重新以阻塞方式
  打开，并在横幅里逐条列出**变了什么**（哪块盘新增 / 移除、哪张卡换了、CPU 换了、
  通道数变了）。标定本身是针对具体机器的，硬件换了之后原来的转速范围未必还成立。

### 变更

- **硬件检测向导改成一页看完**：温度传感器与风扇通道**左右两列并列**，弹窗加宽到
  1240 px；表格、传感器行、折叠标题的字号与内边距一并压紧。窗口窄于 900 px 时自动
  落回单列。

## [1.10.3] - 2026-09-22

### 新增

- **首次使用必须先完成风扇转速标定**。以前向导只是「建议做」，关掉就能进主界面；
  但一路风扇到底听不听话，恰恰是装完之后最难自己判断的事 —— 界面看着在调速、
  风扇却纹丝不动，用户只会以为软件坏了。现在首次启动时向导是**阻塞**的：
  没点过「主动检测」就不能「应用并保存」，关闭按钮隐藏，点遮罩也关不掉。

  唯一的例外：机器上根本没有可调速的 PWM 通道时不再阻塞 —— 那种情况下没有风扇
  会被调错，向导的任务变成把诊断信息讲清楚。

- **硬件检测列出全部温度传感器与风扇通道**。此前只列出能读到数据的那部分，读不到的
  直接消失，分不清「没找到」还是「找到了但没用」。现在：

  - 温度传感器按完整清单列出（coretemp 的每个核心、每块盘的 drivetemp / nvme、
    主板 SuperIO 的每一路输入都算在内）；
  - **没有读数的折叠起来并注明原因** —— 「读数为 0 或无法读取」、
    「读数超出合理范围，按未接传感器处理」（空置的 AUXTIN）、
    「与 CPU 温度重复，CPU 源已包含」（PECI）；
  - 风扇通道同样分成「有转速」与「无转速」两组，后者折叠并提示
    「可能没接风扇，也可能是风扇停转，勾选后主动检测可试出来」。

### 变更

- **温度源面板重新分区**：CPU / 显卡 / 硬盘是三张大卡片；主板、内存、ACPI 热区等
  收进「其它温度源」折叠区，标题上直接写明已启用几路；硬盘逐块温度固定在最下面。

## [1.10.2] - 2026-09-22

### 新增

- **主板、内存等更多温度源**。除 CPU / 显卡 / 硬盘之外，现在还会列出其余可读的
  hwmon 温度输入，**每路一个独立开关**：

  - **主板** —— 风扇控制器（SuperIO）上的 SYSTIN / CPUTIN / T_SENSOR 等；
  - **内存** —— DDR4 走 `jc42`、DDR5 走 `spd5118`，每根内存条一个；
  - **其它** —— ACPI 热区等。

  已经被 CPU / 显卡 / 硬盘源覆盖过的芯片不会重复出现；`PECI` 及其 Calibration
  孪生读数也跳过，因为那就是 CPU 温度。读数超过 105 °C 的输入按「没接传感器」
  处理、不予列出 —— NCT67xx 上空置的 AUXTIN 会稳定读在 111–115 °C，放进来会让
  所有风扇直接拉满。

  新增的传感器默认**只显示读数、不参与调速**，需要在这张温度源卡片上主动打开。

- 打赏入口字体改为**红色**，与旁边的风扇按钮区分开。

### 变更

- **打赏入口恢复为始终显示**。1.10.1 曾把它做成配置项 `show_donate`，可在
  「全局设置」里关掉 —— 方向错了，入口本就应当常驻。该开关、「不再显示」按钮
  与对应的配置项已全部移除。

## [1.10.1] - 2026-09-22

### 变更

- **收款码不再作为独立图片文件随包分发**。此前 `app/ui/images/donate-*.png` 是普通
  文件，在 fpk 里换掉、或者在已安装的应用目录里直接覆盖，都不需要改一行代码。
  现在原图放在仓库的 `assets/donate/`（**不进入打包目录**），构建时由
  `tools/make_donate_qr.py` 编译成内嵌 data URI 的 `app/ui/donate-qr.js`，
  包内不再存在可直接替换的二维码文件。
- 删除 README 与 `donate.json` 中「替换图片即可换成自己的收款码」的说明。

## [1.10.0] - 2026-09-20

### 变更

- **许可证由 MIT 改为 GNU GPL-3.0-or-later**。选择著佐权许可是为了让分发修改版的人
  同样开源，避免被闭源打包进商业产品。LICENSE 使用 FSF 的逐字原文（未添加自定义
  抬头，便于自动化工具识别），全部源码与脚本改用
  `SPDX-License-Identifier: GPL-3.0-or-later` 头。

  > **1.9.0 及更早的版本仍为 MIT，且这一点不可撤销** —— 已经获得那些版本的人永久
  > 保留 MIT 授予的权利，本次变更只对新版本生效。

- 补全全部源码的 SPDX 版权头。此前只有部分文件带版权头（后续重写文件时丢失），
  现在 17 个源码/脚本文件全部一致。

### 新增

- **可选的本机打赏入口**。界面右上角一个 ❤ 按钮，弹出微信 / 支付宝收款码。
  设计上有意做得很克制：
  - **纯本地**：内容来自 `app/ui/donate.json`，不发起任何网络请求、不上报、不统计点击；
  - **不打扰**：只有一个小按钮，绝不自动弹出；
  - **可关闭**：`"enabled": false` 即完全隐藏入口；
  - 二维码图片缺失时显示占位提示而不是破图。

## [1.9.0] - 2026-09-20

### 变更

- **转速标定改为 100 % → 0 % 两点**（此前是 100 % → 30 %，且顺序相反）。
  从 0 % 开始的意义在于：**风扇支持停转时这里会读到 0 RPM**，既能确认 PWM
  真的到达了风扇，也直接给出了可以把「最低转速」设到多低。检测结果里新增
  `stops` 标记，界面显示「0% 可停转」。
- **界面把「占空比」统一改为「转速」**。占空比就是最大转速的百分比，
  最低 / 最高 / 固定 / 失效保护等处一并调整，曲线纵轴也改为「转速 0–100 %」。

### 修复

- **升级或重启后「硬件检测」的标定表格是空的**，看起来就像标定数据丢了。
  配置里的标定其实一直完好（已用完整的卸载重装流程验证：配置文件逐字节一致），
  但检测结果只存在内存中，重启即丢。现在启动时会从配置里回读每路的标定，
  表格直接显示上次的结果与标定时间，并标注「来自已保存的标定」。

### 新增

- `status.fans[]` 现在包含 `calibration` 字段（此前只有 `/api/config` 带）。

## [1.8.0] - 2026-09-20

### 修复

- **硬盘选择跟着"位置"而不是"盘"走**。配置里存的是 `sda`/`sdb` 这类内核设备名，而它们
  只表示"内核先枚举到哪块盘"：换盘位、换数据线、改 BIOS 启动顺序都会改变映射，于是同一个
  `sda` 会静默指向**另一块物理硬盘**，界面却依旧显示它被选中。实测在调整硬盘顺序之后，
  原本选中的 `sda/sdb/sdc` 已经变成了两块 SSD 加一块 HDD。

  现在选择以硬盘自身的标识记录，优先级为：
  1. `/dev/disk/by-id` 名称（udev 生成，如 `ata-WDC_WUH721414ALE6L4_81GDPK2V`）；
  2. 内核 `wwid`（SATA/SAS）或 `serial`（NVMe）；
  3. 兜底才退回设备名。

### 新增

- 硬盘列表每行**同时显示当前设备名与型号/序列号**，两块同型号的 WD 也能靠序列号区分，
  悬停可看到完整稳定标识。
- 旧配置自动迁移：升级时把设备名按**当前对应关系**改写为稳定标识，并逐条记录对应变化。

  > 迁移保留的是**现在**这个名字指向的盘，而不是配置当时的盘 —— 名字一旦变过，原始意图
  > 已无法还原，升级后请核对一遍勾选。

- 硬盘温度源按稳定标识匹配的同时仍接受设备名，未迁移的配置也能继续工作。

### 修复（次要）

- t10 wwid 的填充是**字面的 `\0` 两个字符**（并非 NUL 字节），此前会原样显示成
  `tigo SSD 480GB · \0\0\0\0`，现已正确清理。

## [1.7.1] - 2026-09-20

### 修复

- **重启后打开应用显示 Bad Gateway（502）**。PID 文件存放在持久化目录、会跨重启保留，
  而内核会自由复用 PID。旧版 `cmd/main` 只用 `kill -0` 判断进程存活，于是重启后当旧
  PID 被别的服务（实测是 `trim-sharelink`）占用时，`status` 会误报「应用仍在运行」：
  - 应用中心调用 `cmd/main status` 得到 0，认为无需启动 —— 但它仍会发出
    `APP_AUTO_STARTED` 事件并让网关注册 `/app/niufan` 路由；
  - 结果路由存在、进程不存在、socket 从未创建，用户打开应用只有 502，
    且因为 `status` 不写日志，排查时看不到任何线索。

  现在 `check_process()` 会校验进程身份（比对 `/proc/<pid>/cmdline` 是否为本应用守护
  进程），`status` 还要求 socket 真实存在才报运行中；`start` 会先清理残留的旧进程，
  `stop` 也会扫描 PID 文件之外的游离进程。

### 说明

- 本次故障与 UPS 相关配置无关：NUT 的 5 个服务、`ups.conf` 的 immutable 锁定、
  UPS 管理器应用均未受影响。触发条件是系统重启（UPS 调试期间发生过多次）。

## [1.7.0] - 2026-09-19

### 新增

- **调速诊断**。硬件检测现在会读取 `pwmN_mode`（`0` = DC 电压调速，`1` = PWM 脉宽
  调速），并在**「有转速但调不动」**时逐条给出原因，而不是只显示一个
  「转速无变化」：
  - 写入占空比 X 但读回 Y → 寄存器没接受写入；
  - 写 `pwmN_enable` 被拒绝 → 芯片进不了手动模式，仍由 BIOS 曲线控制；
  - 模式为 DC → 部分主板在此模式下不响应占空比写入，可切到 PWM 后重测；
  - 寄存器读写都正常但转速不变 → 问题在**风扇或接线**（3 针风扇插在 PWM 接头上
    时第 4 根线不起作用，会恒速运转）。
- 硬件检测表格新增「模式」列，直接显示每个通道是 DC 还是 PWM。

### 修复

- **手动模式写入值此前硬编码为 `1`**。NCT6775 家族确实用 `1`，但其它驱动家族
  （以及未来的适配）未必如此，现在改为取自驱动配置的 `profile.manual`。
- **手动模式现在每 30 秒重新下发一次**。部分主板（尤其 ASUS）会周期性重新接管
  SuperIO，此前风扇会悄悄退回 BIOS 曲线，而应用仍自信地以为自己控制着它 ——
  只写一次的模式寄存器无法察觉这种「被夺权」。一次寄存器写入的成本换来自愈能力。

## [1.6.0] - 2026-09-16

### 修复

- **取消勾选显卡后会自动恢复勾选**。旧模型里 `gpu_devices: []` 同时表示「全部显卡」
  和「一个都没选」，于是取消勾选唯一一块卡会被存成空数组，下次渲染又按「全部」
  重新勾上，永远关不掉。新模型给每块卡一个独立的布尔开关，不再依赖「空 = 全部」
  这种有歧义的约定。
- **温度源开关要等一个控制周期才生效**。`enabled` 此前取自上一次轮询的结果，
  改开关后界面最多要等 `interval` 秒才反映。现在直接取自配置，保存即刻生效。

### 变更

- **显卡从「一个 gpu 开关 + 一份设备列表」改为「每块卡一个温度源」**。
  界面上不再是「显卡」卡片加下面一行 `i915`（本质是同一件事却有两个勾选框，
  还一个开一个关），而是：
  - 单卡：一张 **显卡** 卡片，卡内显示型号；
  - 多卡：自动拆成 **显卡1 / 显卡2 …**，各自独立开关，卡内显示各自型号。
- 配置结构随之调整：`sources.gpu` + `sources.gpu_devices` 合并为
  `sources.gpus = {"gpu:<PCI 地址>": bool}`；风扇的 `source` 用
  `"gpu:0000:07:00.0"` 指定具体某块卡。
- **旧配置自动迁移**：`{"gpu": true, "gpu_devices": []}` 会展开为每块卡一个开关，
  风扇源里的 `"gpu"` 也会展开成具体的卡，无需手工改配置。
- 后端新增 `source_list`，由服务端描述温度源（含每块显卡的显示名），
  前端直接渲染该列表，不再需要知道机器上有几块卡。

## [1.5.0] - 2026-09-16

### 修复

- **被关闭的温度源不再直接显示「未启用」**。此前 `sources.gpu = false` 时后端
  根本不读取显卡，界面只显示「– / 未启用」，用户无法区分「这台机器没有该传感器」
  与「你没打开它」。现在 CPU 与显卡**始终读取**，`enabled` 只决定是否参与调速，
  界面显示读数并标注「未参与调速，仅显示读数」。
  （硬盘是例外：读盘可能唤醒休眠硬盘，所以关闭时确实不轮询。）

### 新增

- **显卡按 PCI 型号名识别**。此前只显示驱动名（`i915`、`amdgpu`），在两块卡的
  机器上毫无意义。现在用 `lspci -mm` 解析出型号，显示为
  `Intel DG1 [Iris Xe Graphics] (0000:07:00.0)`；结果带缓存，只在扫描硬件时查询。
- **多显卡支持**：枚举所有可读温度的显卡（内核 hwmon 优先，NVIDIA 经 `nvidia-smi`
  按卡枚举并带 10 秒缓存），配置用 `gpu_devices` 选择参与调速的卡，
  空数组表示取全部卡的最高温。
- 新安装的 `init-config` 会**自动开启本机真正读得到数据的温度源**。

### 说明

- 绑定到 `vfio-pci`（直通给虚拟机）的显卡在内核层面就没有 hwmon 节点，宿主读不到
  温度，这是正常现象而非缺陷。

## [1.4.0] - 2026-09-16

### 修复

- **温度迟滞的棘轮缺陷**。旧实现每个控制周期都会重设「参考温度」，于是判断
  「降够了没有」比较的是**上一个周期**（3 秒前）而不是**产生当前占空比时的温度**。
  由于 CPU 每 3 秒降温远小于迟滞值，条件几乎恒为真，风扇在降温过程中会长时间停在
  远超曲线要求的高转速上。仿真对比同一条温度曲线：旧实现平均偏离曲线 **13.1 个
  占空比**（降温段一直卡在 201，曲线只要求 195→160），修复后为 **2.2**。
- `curve_percent` 诊断字段此前始终为空，现已正确上报，可区分「曲线要求值」与
  「实际下发值」（迟滞或上下限可能让两者不同）。

### 新增

- **两点转速标定**。硬件检测现在分别以约 30 % 与 100 % 占空比各测一次并等待转速
  稳定，给出每路的**最低 / 最高转速**以及「调速有效 / 转速无变化」的结论 ——
  单点只能证明有风扇，两点才能证明 PWM 真的控制得住它。
- **曲线图上的当前工作点**：实时画出「当前温度 × 当前占空比」的落点，直接把
  「风扇为什么跑这么快」显示出来。
- **运行中被动记录转速范围**：不额外试转也会累计每路见过的最低 / 最高转速。

### 变更

- 标定顺序改为**先全速、后低速**：空通道只需一步就能判定，省掉慢得多的降速测量
  （大尺寸机箱风扇降速可能要好几秒）。原先固定等待 1.6 s 会读到上一个转速，
  出现「30 % 时 1730 RPM、100 % 时 1397 RPM」这种反直觉结果。
- `build-fpk.sh` 打包前清理 `__pycache__` / `*.pyc`，避免开发时编译出的字节码
  被打进应用包（曾让包体从 54 KB 膨胀到 82 KB）。

## [1.3.0] - 2026-09-16

### 新增

- **多控制器支持**。不再假定 NCT6775 的寄存器语义，改为按内核文档为每个驱动家族
  建立独立配置（`pwm_enable` 的手动 / 自动取值）。新增适配：
  ITE IT87xx 全系（`it87`）、Fintek F718xx / F8000（`f71882fg`）、
  Fintek F71805F / F71872F（`f71805f`）、Winbond W83627EHF / DHG / W83667HG
  （`w83627ehf`）、Winbond W83627HF / THF / W83697HF（`w83627hf`）、
  SMSC SCH5627 / SCH5636（`sch56xx`）。未匹配到专用配置的 `pwmN` 节点走
  hwmon 通用约定（1=手动、2=自动）兜底。
- **首次启动的硬件自检向导**。列出控制器暴露的**全部** PWM 通道（此前只列出已
  读到转速的通道，导致 6 路控制器只显示 2 路）、每路实测转速、可调速性，以及
  BIOS 为该通道绑定的温度源标签，由用户勾选要纳管的通道。
- **主动通道检测**（`POST /api/probe`）。逐个通道全速试转约 2 秒，识别当时处于
  停转状态的风扇——被动扫描无法区分「空接头」与「停转的风扇」。每路试转后精确
  还原原状态，检测期间控制循环暂停。
- 启动时依次尝试加载 `nct6775` / `it87` / `f71882fg` / `f71805f` / `w83627ehf` /
  `w83627hf` / `sch5627` / `sch5636` / `nct6683`，命中即停，使受支持的主板不再
  因为「模块从来不自动加载」而被误判为不支持。
- 「硬件检测」按钮：任何时候都能重新扫描控制器与实际通道。

### 变更

- **Nuvoton NCT6683 / 6686 / 6687（`nct6683`）识别为仅监控**。内核文档明确说明
  Intel EC 固件的寄存器布局与数据手册不符、从操作系统写入被禁用，因此应用只读取
  其转速与温度，不尝试调速，并在界面上说明原因。
- 交还风扇改为**逐级回退**：优先恢复接管前记录的状态 → 再试驱动自动模式 →
  都不行则给全速。最后一条对不支持自动模式的 ITE 主板是必要的安全网。
- 温度源推断改用驱动提供的**标签**（`tempN_label`）而不是 `temp_sel` 的数值。
  数值本身没有意义：NCT6796D 上 `temp_sel=3` 是空脚 AUXTIN0，早期版本会把它误判
  成 CPU 风扇。

### 修复

- 首次安装只显示部分通道，与实际通道数不符。
- 旧版本升级上来的配置缺少 `setup_complete` 字段时会自动弹出一次自检向导，关闭
  即表示沿用现有配置（通过 `POST /api/action {"action":"skip-setup"}` 记录）。

## [1.2.0] - 2026-09-16

### 修复

- **风扇通道无法移除** —— 界面上只能添加通道、不能删除，误加的空白接头（例如没有
  测速信号的 `FAN3`）会永久留在配置里。风扇卡片右上角现在有「移除」按钮。
- **移除后通道被遗留** —— 即使能从配置里删掉，守护进程也只遍历配置中的风扇，
  被移除的通道会永远停在最后一次写入的手动占空比上。现在移除时会主动交还。
- **交还目标不正确** —— 修复的第一版直接写 `enable=5`（BIOS 自动），但 BIOS 原本
  设为手动档（`enable=1`）的接头会被篡改。现在会在首次接管通道时快照
  `enable` + `duty`，移除时原样还原。
- **停止应用会改写未管理的通道** —— `restore_all_to_auto()` 对所有通道写
  `enable=5`，包括本应用从未碰过的接头。现在守护进程只还原本应用实际驱动过的通道，
  `cmd/main` 也只在不得不 `kill -9` 时才兜底执行全量还原。
- 新增通道时曲线图不再空白：之前 `points: []` 要保存一次才画得出来。

## [1.1.0] - 2026-09-16

### 安全

- **应用 socket 权限由 `0666` 收紧为 `0600`** —— 此前本机任何账号都能通过
  `curl --unix-socket` 绕过飞牛网关直接控制风扇。网关
  （`/usr/trim/bin/trim_http_cgi`）本身以 root 运行，因此 `0600` 不影响正常转发。
- **新增服务端管理员校验** —— 校验网关注入的 `X-Trim-Isadmin`，为假或缺失一律
  返回 `403`（fail-closed）。此前仅依赖 `allUsers: false` 隐藏入口，不构成安全边界。

### 新增

- 未支持风扇控制器探测：若机器上存在带 `pwm*` 属性但不属于 nct6775 系列的
  hwmon 节点（如 `it87`、`f71882fg`、`w83627ehf`），界面会点名列出芯片与路径，
  而不是笼统提示「未找到风扇」。

## [1.0.0] - 2026-09-16

### 新增

- 首个版本。
- CPU / 显卡 / 硬盘三类温度源，多源取最高值。
- 每风扇独立的温度曲线、最低 / 最高占空比、温度迟滞。
- 温度曲线 / 固定转速 / BIOS 自动三种模式。
- 配置原子持久化，卸载不删除。
- 飞牛统一网关接入，不开放 TCP 端口，原生 JS 界面。
- 失效保护与停止 / 卸载自动交还 BIOS。

[1.10.0]: ../../compare/v1.9.0...v1.10.0
[1.9.0]: ../../compare/v1.8.0...v1.9.0
[1.8.0]: ../../compare/v1.7.1...v1.8.0
[1.7.1]: ../../compare/v1.7.0...v1.7.1
[1.7.0]: ../../compare/v1.6.0...v1.7.0
[1.6.0]: ../../compare/v1.5.0...v1.6.0
[1.5.0]: ../../compare/v1.4.0...v1.5.0
[1.4.0]: ../../compare/v1.3.0...v1.4.0
[1.3.0]: ../../compare/v1.2.0...v1.3.0
[1.2.0]: ../../compare/v1.1.0...v1.2.0
[1.1.0]: ../../compare/v1.0.0...v1.1.0
[1.0.0]: ../../releases/tag/v1.0.0
